Running the Java Backend server:
./mvnw spring-boot:run

To Package the program:
./mvnw clean package

Running the Frontend server:

npm install
npm start

To update Output.css from tailwind during development:
npx tailwindcss -i src/styles/index.css -o src/styles/output.css --watch
